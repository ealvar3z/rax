--- DESIGN.md
+++ DESIGN.md
@@ -1305,7 +1305,7 @@
 
 Each patch must:
 
-- apply with the macOS `patch` utility;
+- apply with either the macOS `patch` utility or Git's `git apply` command;
 - contain one architectural move;
 - leave the tree testable;
 - avoid code for later milestones;
